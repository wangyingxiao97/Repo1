server:
  port: 10086
spring:
  application:
    name: ly-registry
eureka:
  client:
    fetch-registry: false # 取消注册自己
    register-with-eureka: false # 取消注册自己
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
  server:
    enable-self-preservation: false # 关闭注册中心的保护机制
